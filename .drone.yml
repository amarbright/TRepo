---
pipeline:
  build:
    when:
      event:
      - push
      - pull_request
    image: maven:3-jdk-8
    commands:
    - mvn -B clean package
  publish:
    artifactory:
      url: http://localhost:8081/artifactory/automation_local_maven/automation/automation-0.0.1-SNAPSHOT.jar
      username: admin
      password: asiya123 
      files: 
        - /drone/src/github.com/amarbright/TRepo/target/*.jar
        - /drone/src/github.com/amarbright/TRepo/target/*.war
      force_upload: true
    notify:
    image: plugins/slack
    webhook: https://hooks.slack.com/services/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
    channel: automation
    username: drone
    template: >
      {{#success build.status}}
      Hey Amar, build {{build.number}} succeed & moved to Repo...
      {{else}}
      Hey Amar, build {{build.number}} Failed, fix me...
      {{/success}}
